# server get cpu temp
# - platform: ssh
#   name: Proxmox Server CPU Temperatur # Dell Optiplex 790
#   host: !secret ip_pve_server
#   username: !secret pve_username
#   password: !secret pve_password
#   command: sensors | grep 'Package id 0:' | cut -c17-20
#   value_template: >-
#     {%- set line = value.split("\r\n") -%}
#     {{ line[1] }}
#   unit_of_measurement: !secret unit_temperature


## auto shutdown if ups is primary power source
# shutdown main server
#- platform: ssh
#  name: Proxmnox Server Herunterfahren
#  host: !secret ip_pve_server
#  username: !secret pve_username
#  password: !secret pve_password
#  command: shutdown now
#  #value_template: "{{ binary_sensor.lokaler_proxmox_server }}"
#  unit_of_measurement: ""

# shutdown dns server
#- platform: ssh
#  name: NanoPi DNS Server Herunterfahren
#  host: !secret ip_dns_server
#  username: !secret remote_dns-server_user
#  password: !secret remote_dns-server_pass
#  command: shutdown now
#  unit_of_measurement: ""
