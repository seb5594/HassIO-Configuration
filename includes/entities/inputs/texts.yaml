wifi_main_ssid:
  initial: !secret wifi_main_ssid

wifi_main_pass:
  initial: !secret wifi_main_pass
  mode: password

wifi_guest_ssid:
  initial: !secret wifi_guest_ssid

wifi_guest_pass:
  initial: !secret wifi_guest_pass
  mode: password
